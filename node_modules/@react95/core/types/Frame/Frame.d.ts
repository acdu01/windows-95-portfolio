import type { ElementType, ForwardedRef, Ref, ReactNode, RefAttributes, ReactElement, PropsWithChildren, ComponentPropsWithoutRef } from 'react';
import { Sprinkles } from './Frame.css';
type As = ElementType;
type Props = Record<string, unknown>;
export type Polymorphic<E extends As, P = Props> = P & PropsWithChildren<{
    as?: E;
}> & ComponentPropsWithoutRef<E>;
type InferredElement<C extends ElementType> = C extends keyof HTMLElementTagNameMap ? HTMLElementTagNameMap[C] : C extends keyof SVGElementTagNameMap ? SVGElementTagNameMap[C] : HTMLElement;
type FixedForwardRef = <T, P = object>(render: (props: P, ref: Ref<T>) => ReactNode) => (props: P & RefAttributes<T>) => ReactNode;
export declare const fixedForwardRef: FixedForwardRef;
export type FrameProps = Sprinkles;
export declare const Frame: <E extends As>(p: Polymorphic<E, FrameProps> & {
    ref?: ForwardedRef<InferredElement<E>>;
}) => ReactElement | null;
export {};
